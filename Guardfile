# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'sass', :input => 'bower_components/foundation/scss', :output => 'public/css' do
  watch(%r{^sass/.+(\.sass)$})
end

guard "slim" do
  watch(%r{^templates/.+(\.slim)$})
end
